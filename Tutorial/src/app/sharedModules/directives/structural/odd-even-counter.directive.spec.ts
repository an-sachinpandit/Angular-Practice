import { OddEvenCounterDirective } from './odd-even-counter.directive';

describe('OddEvenCounterDirective', () => {
  it('should create an instance', () => {
    const directive = new OddEvenCounterDirective();
    expect(directive).toBeTruthy();
  });
});
